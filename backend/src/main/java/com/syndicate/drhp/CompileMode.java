package com.syndicate.drhp;

public enum CompileMode {
    /** Ordinary compile for review. */
    DRAFT_PREVIEW,
    /** The filing copy: produces a cryptographic provenance manifest. */
    FINAL_FILING
}
